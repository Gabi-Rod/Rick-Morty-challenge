import { useEffect } from "react";
import { useAppDispatch, useAppSelector } from "../../app/hooks";

export function EpisodeItem(props: any) {
  const dispatch = useAppDispatch();

  return (
    <>
      <span>{}</span>
    </>
  );
}
